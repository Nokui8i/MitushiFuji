{%- liquid
  assign loading = "lazy"
  assign decoding ="async"
  assign fetchpriority = "auto"
  assign image_classes = 'w-100 h-100 js-animate anm anm-zoomOut'
  if section.index == 1
    assign loading = "eager"
    assign fetchpriority = "high"
    assign image_classes = 'w-100 h-100'
  endif
  assign primary_image = section.settings.primary_image
  assign secondary_image = section.settings.secondary_image
  assign primary_image_ratio = section.settings.primary_image_ratio
  assign secondary_image_ratio = section.settings.secondary_image_ratio
  assign heading_style = section.settings.enable_large_heading | default: 'ts-default' | replace: 'true', 'headline ts-default'

  assign media_width_desktop = 'calc(100vw / 2)'

  if settings.enable_content_max_width
    assign media_width_desktop = 'calc(min(100vw, ' | append: settings.content_max_width | append: 'px) / 2)'
  endif

  assign sizes = '(min-width: 769px) ' | append: media_width_desktop | append: ', ' | append: '100vw'
  assign widths = '240, 352, 550, 832, 1200, 1600, 1920, 2560, 3840'


  assign placeholder = 'detailed-apparel-1' | placeholder_svg_tag: 'h-100 w-100 placeholder media of-cover js-image-lazy'
  assign primary_image_alt = primary_image.alt | default: primary_image.image | split: '/' | last
  assign primary_media = primary_image | image_url: width: primary_image.width | image_tag: widths: widths, sizes: sizes, alt: primary_image_alt, class: 'js-image-lazy media of-cover w-100 h-100', loading: loading, fetchpriority: fetchpriority, decoding: decoding

  assign secondary_media_alt = secondary_image.alt | default: secondary_image.image | split: '/' | last
  assign secondary_media = secondary_image | image_url: width: secondary_image.width | image_tag: widths: widths, sizes: sizes, alt: secondary_media_alt, class: 'js-image-lazy media of-cover w-100 h-100', loading: 'lazy', fetchpriority: 'auto', decoding: decoding
  assign classes = 's-header s-header--special pt-zero-mb zi-1 d-flex fd-column'

  if section.settings.content_position == 'middle'
    assign classes = 's-header s-header--special pt-zero-mb zi-1 d-flex fd-column center-xs'
  endif
  if section.settings.content_position == 'bottom'
    assign classes = 's-header s-header--special pt-zero-mb zi-1 d-flex fd-column end-xs'
  endif


  assign primary_img_style = primary_image_ratio | replace: 'landscape', '--gc-xs: 1/6; --gr-xs: 1/5;' | replace: 'square', '--gc-xs: 1/6; --gr-xs: 1/6;' | replace: 'portrait', '--gc-xs: 2/6; --gr-xs: 1/6;'
  assign secondary_img_style = secondary_image_ratio | replace: 'landscape', '--gc-xs: 1/6; --gr-xs: 1/5;' | replace: 'square', '--gc-xs: 1/6; --gr-xs: 1/6;' | replace: 'portrait', '--gc-xs: 2/6; --gr-xs: 1/6;'

  if primary_image != blank and secondary_image != blank
    assign primary_img_style = '--gc-xs: 2/6; --gr-xs: 1/4;'
    assign secondary_img_style = secondary_image_ratio | replace: 'portrait', '--gc-xs: 1/3; --gr-xs: 3/6;' | replace: 'square', '--gc-xs: 1/3; --gr-xs: 3/5;' | replace: 'landscape', '--gc-xs: 1/4; --gr-xs: 3/5;'

    if primary_image_ratio == 'portrait'
      assign primary_img_style = '--gc-xs: 3/6; --gr-xs: 1/5;'
      assign secondary_img_style = secondary_image_ratio | replace: 'portrait', '--gc-xs: 2/4; --gr-xs: 3/6;' | replace: 'square', '--gc-xs: 2/4; --gr-xs: 4/6;' | replace: 'landscape', '--gc-xs: 1/4; --gr-xs: 4/6;'
    elsif primary_image_ratio == 'square'
      assign primary_img_style = '--gc-xs: 2/6; --gr-xs: 1/5;'
      assign secondary_img_style = secondary_image_ratio | replace: 'portrait', '--gc-xs: 1/3; --gr-xs: 3/6;' | replace: 'square', '--gc-xs: 1/3; --gr-xs: 4/6;' | replace: 'landscape', '--gc-xs: 1/4; --gr-xs: 4/6;'
    endif
  endif

  assign header_style = section.settings.content_alignment | replace: 'ta-right', '--gc-xs: 1/13; --gc-md: 1/8; --gr-xs: 1;' | replace: 'ta-center', '--gc-md: 3/11; --gc-xs: 1/13; --gr-xs: 1' | replace: 'ta-left', '--gc-xs: 1/13; --gc-md: 7/13; --gr-xs: 1;'
  assign media_style = section.settings.content_alignment | replace: 'ta-right', '--gc-xs: 1/13; --gc-md: 7/13; --gr-xs: 2; --gr-md: 1;' | replace: 'ta-center', '--gc-xs: 1/13; --gc-md: 3/11;' | replace: 'ta-left', '--gc-xs: 1/13; --gc-md: 1/7; --gr-xs: 2; --gr-md: 1;'
  assign class_swap = ''
  if section.settings.content_alignment == 'ta-center'
    assign description_extra_class = ' m-sides'
    assign btn_position = 'center-xs'
    assign classes = classes | replace: 's-header--special', ''
  elsif section.settings.content_alignment == 'ta-right'
    assign class_swap = 'd-rtl-md'
  elsif section.settings.content_alignment == 'ta-left'
    assign class_swap = 'd-rtl'
  endif

  if settings.enable_right_to_left
    assign class_swap = class_swap | replace: 'rtl', 'ltr'
  endif
-%}
<div class="section image-with-text color-{{- section.settings.color_scheme }} {{ section.settings.remove_padding_top | replace: 'true', 'section-top-zero' | replace: section.settings.remove_padding_top, '' }} {{ section.settings.remove_padding_bottom | replace: 'true', 'section-bottom-zero' | replace: section.settings.remove_padding_bottom, '' }} {% render 'animation-loader', section: section %}" data-ui-component="Image With Text Section" role="region" aria-labelledby="sectionHeading-{{ section.id | md5 | truncate: 12, "" }}">
  <div class="ctnr d-grid c-gap {{ section.settings.content_alignment | replace: 'ta-right', 'iwt-content--right' }}" style="--gtc-equal:12">
    {% render 'section-header',
      btn_position: btn_position,
      classes: classes,
      content_align: section.settings.content_alignment,
      header_style: header_style,
      subheading: section.settings.subheading,
      heading: section.settings.heading,
      description: section.settings.description,
      description_extra_class: description_extra_class,
      heading_classes: heading_style,
      button_main_text: section.settings.button_main_text,
      button_main_link: section.settings.button_main_link,
      button_sub_text: section.settings.button_sub_text,
      button_sub_link: section.settings.button_sub_link,
      badge_text: section.settings.badge_text,
      badge_position: section.settings.badge_position,
      badge_shape: section.settings.badge_shape,
      badge_color: section.settings.badge_color,
      section: section %}
    <div style="{{ media_style }}">
      <div class="section-content__media p-relative">
        <div class="p-absolute left-0 top-0 d-grid h-100 w-100 {{ class_swap }}" style="--gtc-equal: 5; --gtr-equal: 5">
          {% if primary_image %}
            <div class="o-hidden" style="{{ primary_img_style }}">
              {% render 'image', image_item: primary_media, image_wrap_classes: 'h-100', image_classes: image_classes, media_wrap_style: '--delay: .6s', order: section.index %}
            </div>
          {% endif %}
          {% if secondary_image %}
            <div class="f-default o-hidden" style="{{ secondary_img_style }}">
              {% render 'image', image_item: secondary_media, image_wrap_classes: 'h-100', image_classes: image_classes, media_wrap_style: '--delay: .6s', order: section.index %}
            </div>
          {% endif %}
          {% if primary_image == blank and secondary_image == blank %}
            <div style="{{ primary_img_style }}">
              {% render 'image', image_item: placeholder, image_wrap_classes: 'h-100', image_classes: 'd-block h-100 js-animate anm anm-zoomOut', order: section.index %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:general.image_with_text",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [
    {
      "type": "header",
      "content": "t:general.general"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:general.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:general.layout"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "t:general.image_position",
      "default": "ta-right",
      "options": [
        {
          "value": "ta-left",
          "label": "t:general.left"
        },
        {
          "value": "ta-right",
          "label": "t:general.right"
        },
        {
          "value": "ta-center",
          "label": "t:general.center"
        }
      ]
    },
    {
      "type": "select",
      "id": "content_position",
      "label": "t:general.content_alignment",
      "default": "middle",
      "options": [
        {
          "value": "top",
          "label": "t:general.top"
        },
        {
          "value": "middle",
          "label": "t:general.middle"
        },
        {
          "value": "bottom",
          "label": "t:general.bottom"
        }
      ],
      "visible_if": "{{ section.settings.content_alignment != 'ta-center' }}"
    },
    {
      "type": "checkbox",
      "id": "remove_padding_top",
      "label": "t:general.remove_padding_top",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "remove_padding_bottom",
      "label": "t:general.remove_padding_bottom",
      "default": true
    },
    {
      "type": "header",
      "content": "t:general.image"
    },
    {
      "type": "image_picker",
      "id": "primary_image",
      "label": "t:general.primary_image"
    },
    {
      "type": "select",
      "id": "primary_image_ratio",
      "label": "t:general.image_ratio",
      "default": "square",
      "options": [
        {
          "value": "square",
          "label": "t:general.square"
        },
        {
          "value": "portrait",
          "label": "t:general.portrait"
        },
        {
          "value": "landscape",
          "label": "t:general.landscape"
        }
      ],
      "visible_if": "{{ section.settings.primary_image != blank }}"
    },
    {
      "type": "image_picker",
      "id": "secondary_image",
      "label": "t:general.secondary_image"
    },
    {
      "type": "select",
      "id": "secondary_image_ratio",
      "label": "t:general.image_ratio",
      "default": "portrait",
      "options": [
        {
          "value": "square",
          "label": "t:general.square"
        },
        {
          "value": "portrait",
          "label": "t:general.portrait"
        },
        {
          "value": "landscape",
          "label": "t:general.landscape"
        }
      ],
      "visible_if": "{{ section.settings.secondary_image != blank }}"
    },
    {
      "type": "header",
      "content": "t:general.text"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "t:general.subheading",
      "default": "Subheading"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:general.heading",
      "default": "Image with text"
    },
    {
      "type": "checkbox",
      "id": "enable_large_heading",
      "label": "t:general.enable_large_heading",
      "info": "t:general.enable_large_heading_info",
      "visible_if": "{{ section.settings.heading != blank }}"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "t:general.description",
      "default": "<p>Use this text to share brand information with your customers. Introduce a new product, share an announcement or welcome customers.</p>"
    },
    {
      "type": "header",
      "content": "t:general.buttons"
    },
    {
      "type": "text",
      "id": "button_main_text",
      "label": "t:general.primary_button_label",
      "info": "t:general.hide_if_empty_info",
      "default": "Button label"
    },
    {
      "type": "url",
      "id": "button_main_link",
      "label": "t:general.primary_button_link",
      "visible_if": "{{ section.settings.button_main_text != blank }}"
    },
    {
      "type": "text",
      "id": "button_sub_text",
      "label": "t:general.secondary_button_label",
      "info": "t:general.hide_if_empty_info",
      "default": "Button label"
    },
    {
      "type": "url",
      "id": "button_sub_link",
      "label": "t:general.secondary_button_link",
      "visible_if": "{{ section.settings.button_sub_text != blank }}"
    },
    {
      "type": "header",
      "content": "t:general.prominent_badge"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "t:general.text",
      "info": "t:general.prominent_badge_text_info",
      "default": "Get ready"
    },
    {
      "type": "select",
      "id": "badge_color",
      "label": "t:general.accent_scheme",
      "default": "color-accent-1",
      "visible_if": "{{ section.settings.badge_text != blank }}",
      "options": [
        {
          "value": "color-accent-1",
          "label": "t:general.main"
        },
        {
          "value": "color-accent-2",
          "label": "t:general.extra"
        }
      ]
    },
    {
      "type": "select",
      "id": "badge_position",
      "label": "t:general.position",
      "default": "p-badge--bottom-right",
      "visible_if": "{{ section.settings.badge_text != blank }}",
      "options": [
        {
          "value": "p-badge--bottom-left",
          "label": "t:general.left"
        },
        {
          "value": "p-badge--bottom-right",
          "label": "t:general.right"
        }
      ]
    },
    {
      "type": "select",
      "id": "badge_shape",
      "label": "t:general.shape",
      "default": "badge--star",
      "visible_if": "{{ section.settings.badge_text != blank }}",
      "options": [
        {
          "value": "badge--star",
          "label": "t:general.star"
        },
        {
          "value": "badge--diamond",
          "label": "t:general.diamond"
        },
        {
          "value": "badge--asterisk",
          "label": "t:general.asterisk"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:general.image_with_text"
    }
  ]
}
{% endschema %}
