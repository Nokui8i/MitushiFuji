{%- liquid

  assign loading = "lazy"
  assign decoding ="async"
  assign fetchpriority = "auto"

  if section.index == 1
    assign loading = "eager"
    assign fetchpriority = "high"
  endif

  assign min_height_mobile = section.settings.height_mobile
  assign min_height_desktop = section.settings.height_desktop
  if min_height_desktop == "adapt_img"
    if section.settings.desktop_background_image == blank
      assign min_height_desktop = "400px"
    else
      assign min_height_desktop = 1 | divided_by: section.settings.desktop_background_image.aspect_ratio | times: 100 | append: 'vw'
    endif
  endif

  if min_height_mobile == "adapt_img"
    if section.settings.mobile_background_image != blank
      assign min_height_mobile = 1 | divided_by: section.settings.mobile_background_image.aspect_ratio | times: 100 | append: 'vw'
    else
      assign min_height_mobile = 1 | divided_by: section.settings.desktop_background_image.aspect_ratio | times: 100 | append: 'vw'
    endif
  endif

  unless section.settings.section_mask_overlay_color.alpha == 0.0 or section.settings.section_mask_overlay_color == blank
    assign overlay_color =  '--color-page-bg-rgb:' | append: section.settings.section_mask_overlay_color.red | append: ',' | append: section.settings.section_mask_overlay_color.green | append: ',' | append: section.settings.section_mask_overlay_color.blue
  endunless
  unless section.settings.section_content_overlay_color.alpha == 0.0 or section.settings.section_content_overlay_color == blank
    assign content_color =  'color:' | append: section.settings.section_content_overlay_color
  endunless
  unless section.blocks[0].settings.color_end_text.alpha == 0.0 or section.blocks[0].settings.color_end_text == blank
    assign message_color = '--color-countdown-message:' | append: section.blocks[0].settings.color_end_text
  endunless
  assign fullwidth_banner_class = 'ctnr-banner'
  assign padding_banner_class = ''
  if section.settings.enable_fullwidth_banner == true
    assign fullwidth_banner_class = 'w-100'
  endif
  if section.settings.enable_fullwidth_banner == false
    assign padding_banner_class = 'banner-wrapper'
  endif
-%}
{% capture countdown %}
  {% if section.blocks[0].settings.enable_countdown_timer == true %}
    {%- liquid
      assign is_expried = false
      assign hide_countdown_cls = ''
      assign hide_countdown_message_cls = 'd-none'
      assign today = "now" | date: "%Y-%m-%d"
      assign end_time = section.blocks[0].settings.end_time | default: '23:59' | append: ':00'
      assign end_date = section.blocks[0].settings.end_date | default: today

      assign timezone_offset = "now" | date: "%z"
      assign expired_at = end_date | append: ' ' | append: end_time | append: ' ' | append: timezone_offset

      assign expired_at_timestamp = expired_at | date: '%s'
      assign now_timestamp = 'now' | date: '%s'

      assign diff_seconds = expired_at_timestamp | minus: now_timestamp

      assign days = diff_seconds | divided_by: 3600 | divided_by: 24 | round
      assign hours = diff_seconds | modulo: 86400 | divided_by: 3600 | round
      assign minutes = diff_seconds | modulo: 3600 | divided_by: 60 | round
      assign seconds = diff_seconds | modulo: 60 | round

      assign days_string = '00'
      assign hours_string = '00'
      assign minutes_string = '00'
      assign seconds_string = '00'

      if diff_seconds > 0 and days < 9 and days >= 0
        assign days_string = days | prepend: '0'
      elsif diff_seconds > 0 and days > 9
        assign days_string = days
      endif

      if diff_seconds > 0 and hours < 9 and hours >= 0
        assign hours_string = hours | prepend: '0'
      elsif diff_seconds > 0 and hours > 9
        assign hours_string = hours
      endif

      if diff_seconds > 0 and minutes < 9 and minutes >= 0
        assign minutes_string = minutes | prepend: '0'
      elsif diff_seconds > 0 and minutes > 9
        assign minutes_string = minutes
      endif

      if diff_seconds > 0 and seconds < 9 and seconds >= 0
        assign seconds_string = seconds | prepend: '0'
      elsif diff_seconds > 0 and seconds > 9
        assign seconds_string = seconds
      endif

      if diff_seconds <= 0
        assign is_expried = true
        if section.blocks[0].settings.hide_on_end
          assign hide_countdown_cls = "d-none-important"
        endif
        if section.blocks[0].settings.end_text != blank
          assign hide_countdown_message_cls = ''
        endif
      endif
    -%}
  <sht-countdown class="d-block" data-is-expired="{{ is_expried }}" data-hide-on-end="{{ section.blocks[0].settings.hide_on_end }}" data-time-zone-name="{{ "now" | date: "%Z" }}" data-time-zone-offset="{{ timezone_offset }}" data-end-time="{{ end_time }}" data-end-date="{{ end_date }}">
    <time class="countdown_counter js-counter d-inline-flex gap-24 fw-wrap {{ hide_countdown_cls }} {%- if section.blocks[0].settings.content_position contains 'ta-center' -%} center-xs {%- endif -%}">
      <span class="d-flex fd-column middle-xs gap-8">
        <span class="h3 m-zero js-counter-days">{{ days_string }}</span>
        <span>{{ 'sections.countdown_banner.days' | t }}</span>
      </span>
      <span class="d-flex fd-column middle-xs gap-8">
        <span class="h3 m-zero js-counter-hours">{{ hours_string }}</span>
        <span>{{ 'sections.countdown_banner.hours' | t }}</span>
      </span>
      <span class="d-flex fd-column middle-xs gap-8">
        <span class="h3 m-zero js-counter-minutes">{{ minutes_string }}</span>
        <span>{{ 'sections.countdown_banner.minutes' | t }}</span>
      </span>
      <span class="d-flex fd-column middle-xs gap-8">
        <span class="h3 m-zero js-counter-seconds">{{ seconds_string }}</span>
        <span>{{ 'sections.countdown_banner.seconds' | t }}</span>
      </span>
    </time>
    <div class="countdown_message text-block {{ hide_countdown_message_cls }} js-counter-message" style="{{ message_color }}">{{- section.blocks[0].settings.end_text -}}</div>
  </sht-countdown>
  {% endif %}
{% endcapture %}

<div class="p-relative color-{{- section.settings.color_scheme }} {% render 'animation-loader', section: section %} h-100 content--special section {{ section.settings.remove_padding_top | replace: 'true', 'section-top-zero' | replace: section.settings.remove_padding_top, '' }} {{ section.settings.remove_padding_bottom | replace: 'true', 'section-bottom-zero' | replace: section.settings.remove_padding_bottom, '' }}" data-ui-component="Countdown Banner Section" role="region" aria-labelledby="sectionHeading-{{ section.id | md5 | truncate: 12, "" }}">
  <div class="p-relative {{ fullwidth_banner_class }} {{ padding_banner_class }}">
    {%- if section.settings.image_banner_link != blank %}
      {% unless section.blocks[0].settings.primary_button_label != blank and section.blocks[0].settings.primary_button_link != blank %}
        <a href="{{- section.settings.image_banner_link -}}" class="p-absolute top-0 right-0 bottom-0 left-0 zi-2" target="_blank" aria-label="{{- section.blocks[0].settings.heading |  escape -}}"></a>
      {% endunless %}
    {%- endif -%}
    <div class="p-absolute h-100 w-100 left-0 top-0 {{ padding_banner_class }}">
      <div class="sht-mask p-relative w-100 h-100 o-hidden" style="--overlay: {{ section.settings.section_overlay_opacity }}; {{ overlay_color }}">
        <div class="d-block w-100 h-100 js-animate anm anm-zoomOut media-wrapper">
          {% render 'picture', settings: section.settings, loading: loading, fetchpriority: fetchpriority, decoding: decoding %}
        </div>
      </div>
    </div>
    <div class="ctnr zi-1 w-100 h-100 mah-100 p-relative d-flex fd-column banner-padding {{ section.blocks[0].settings.content_position }}" {{ section.blocks[0].shopify_attributes }} style="--mh-xs: {{- min_height_mobile -}};--mh-sm: {{- min_height_desktop -}}; {{content_color}}">
      {%- assign heading_style = section.settings.enable_large_heading | default: '' | replace: 'true', 'headline' -%}
      {%- render 'section-header'
        classes: 's-header mah-100 pb-0 w-100',
        header_content_class: 'mah-100 o-hidden',
        content_align: section.blocks[0].settings.content_position,
        subheading: section.blocks[0].settings.subheading,
        heading: section.blocks[0].settings.heading,
        heading_classes: heading_style,
        description: section.blocks[0].settings.description,
        button_main_text: section.blocks[0].settings.primary_button_label,
        button_main_link: section.blocks[0].settings.primary_button_link,
        badge_text: section.settings.badge_text,
        badge_position: section.settings.badge_position,
        badge_shape: section.settings.badge_shape,
        badge_color: section.settings.badge_color,
        advance_content: countdown,
        section: section
      -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:general.countdown_banner",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [
    {
      "type": "header",
      "content": "t:general.general"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:general.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "enable_large_heading",
      "label": "t:general.enable_large_heading",
      "info": "t:general.enable_large_heading_info",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_fullwidth_banner",
      "label": "t:general.enable_fullwidth_banner",
      "default": true
    },
    {
      "type": "range",
      "unit": "%",
      "max": 90,
      "min": 0,
      "step": 1,
      "default": 30,
      "id": "section_overlay_opacity",
      "label": "t:general.mask_overlay_opacity"
    },
    {
      "type": "color",
      "id": "section_mask_overlay_color",
      "label": "t:general.mask_overlay",
      "info": "t:general.banner_mask_overlay_info",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "section_content_overlay_color",
      "label": "t:general.content_overlay",
      "info": "t:general.banner_content_overlay_info",
      "default": "#FFFFFF"
    },
    {
      "type": "header",
      "content": "t:general.layout"
    },
    {
      "type": "select",
      "label": "t:general.section_height_on_large_screen",
      "id": "height_desktop",
      "info": "t:general.section_height_info",
      "options": [
        {
          "value": "50vh",
          "label": "t:general.height_50_percent"
        },
        {
          "value": "70vh",
          "label": "t:general.height_70_percent"
        },
        {
          "value": "100vh",
          "label": "t:general.fill_screen_height"
        },
        {
          "value": "adapt_img",
          "label": "t:general.adapt_to_image"
        }
      ],
      "default": "70vh"
    },
    {
      "type": "select",
      "label": "t:general.section_height_on_phone",
      "id": "height_mobile",
      "info": "t:general.section_height_info",
      "options": [
        {
          "value": "50vh",
          "label": "t:general.height_50_percent"
        },
        {
          "value": "70vh",
          "label": "t:general.height_70_percent"
        },
        {
          "value": "100vh",
          "label": "t:general.fill_screen_height"
        },
        {
          "value": "adapt_img",
          "label": "t:general.adapt_to_image"
        }
      ],
      "default": "70vh"
    },
    {
      "type": "checkbox",
      "id": "remove_padding_top",
      "label": "t:general.remove_padding_top",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "remove_padding_bottom",
      "label": "t:general.remove_padding_bottom",
      "default": true
    },
    {
      "type": "header",
      "content": "t:general.image"
    },
    {
      "type": "image_picker",
      "label": "t:general.background_image",
      "id": "desktop_background_image",
      "info": "t:general.desktop_image_recommend_info"
    },
    {
      "type": "image_picker",
      "label": "t:general.mobile_background_image",
      "id": "mobile_background_image",
      "info": "t:general.mobile_image_recommend_info"
    },
    {
      "type": "header",
      "content": "t:general.prominent_badge"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "t:general.text",
      "info": "t:general.prominent_badge_text_info",
      "default": "Get ready"
    },
    {
      "type": "select",
      "id": "badge_color",
      "label": "t:general.accent_scheme",
      "default": "color-accent-1",
      "visible_if": "{{ section.settings.badge_text != blank }}",
      "options": [
        {
          "value": "color-accent-1",
          "label": "t:general.main"
        },
        {
          "value": "color-accent-2",
          "label": "t:general.extra"
        }
      ]
    },
    {
      "type": "select",
      "id": "badge_position",
      "label": "t:general.position",
      "default": "p-badge--top-right",
      "visible_if": "{{ section.settings.badge_text != blank }}",
      "options": [
        {
          "value": "p-badge--top-left",
          "label": "t:general.left"
        },
        {
          "value": "p-badge--top-right",
          "label": "t:general.right"
        }
      ]
    },
    {
      "type": "select",
      "id": "badge_shape",
      "label": "t:general.shape",
      "default": "badge--star",
      "visible_if": "{{ section.settings.badge_text != blank }}",
      "options": [
        {
          "value": "badge--star",
          "label": "t:general.star"
        },
        {
          "value": "badge--diamond",
          "label": "t:general.diamond"
        },
        {
          "value": "badge--asterisk",
          "label": "t:general.asterisk"
        }
      ]
    },
    {
      "type": "header",
      "content": "t:general.link"
    },
    {
      "type": "url",
      "id": "image_banner_link",
      "label": "t:general.link",
      "info": "t:general.banner_link_info"
    }
  ],
  "blocks": [
    {
      "type": "content",
      "name": "t:general.content",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "label": "t:general.content_position",
          "id": "content_position",
          "options": [
            {
              "value": "center-xs middle-xs ta-center",
              "label": "t:general.center"
            },
            {
              "value": "center-xs top-xs ta-left",
              "label": "t:general.left_center"
            },
            {
              "value": "end-xs top-xs ta-left",
              "label": "t:general.left_bottom"
            }
          ],
          "default": "center-xs middle-xs ta-center"
        },
        {
          "type": "header",
          "content": "t:general.text"
        },
        {
          "type": "text",
          "label": "t:general.subheading",
          "id": "subheading",
          "default": "Subheading"
        },
        {
          "type": "text",
          "label": "t:general.heading",
          "id": "heading",
          "default": "Countdown banner"
        },
        {
          "type": "richtext",
          "label": "t:general.description",
          "id": "description",
          "default": "<p>Share information about your promotion.</p>"
        },
        {
          "type": "header",
          "content": "t:general.countdown",
          "info": "t:general.countdown_info"
        },
        {
          "type": "checkbox",
          "id": "enable_countdown_timer",
          "label": "t:general.enable_countdown_timer",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "hide_on_end",
          "label": "t:general.hide_on_end",
          "info": "t:general.hide_on_end_info",
          "default": true
        },
        {
          "type": "text",
          "id": "end_date",
          "label": "t:general.end_date",
          "info": "t:general.end_date_info"
        },
        {
          "type": "text",
          "id": "end_time",
          "label": "t:general.end_time",
          "info": "t:general.end_time_info"
        },
        {
          "type": "richtext",
          "id": "end_text",
          "label": "t:general.end_text",
          "info": "t:general.end_text_info",
          "default": "<p>This event is finished.</p>"
        },
        {
          "type": "color",
          "id": "color_end_text",
          "label": "t:general.end_text",
          "default": "#FF0000"
        },
        {
          "type": "header",
          "content": "t:general.button"
        },
        {
          "type": "text",
          "id": "primary_button_label",
          "label": "t:general.label",
          "info": "t:general.hide_if_empty_info"
        },
        {
          "type": "url",
          "id": "primary_button_link",
          "label": "t:general.link",
          "visible_if": "{{ block.settings.primary_button_label != blank }}"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:general.countdown_banner",
      "blocks": [
        {
          "type": "content"
        }
      ]
    }
  ]
}
{% endschema %}